# OpenAI API 키 (선택 사항 – 없으면 AI 분석을 건너뜁니다)
OPENAI_API_KEY=

# 사용할 OpenAI 모델 (기본값: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# 설정 파일 경로 (기본값: config.yaml)
NEET_CONFIG=config.yaml

# SQLite DB 경로 (config.yaml의 database.path를 덮어씀)
NEET_DB_PATH=data/neet_articles.db
